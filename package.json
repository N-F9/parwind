{
	"scripts": {
		"format": "npx prettier --write .",
		"start": "npm run format && rm -rf dist .cache .parcel-cache && parcel src/*.html src/**/*.html",
		"build": "parcel build src/*.html src/**/*.html"
	},
	"devDependencies": {
		"@parcel/packager-raw-url": "^2.6.2",
		"@parcel/transformer-webmanifest": "^2.6.2",
		"parcel": "^2.6.2",
		"postcss": "^8.4.14",
		"prettier": "^2.7.1",
		"tailwindcss": "^3.1.6"
	}
}
